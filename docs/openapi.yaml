openapi: 3.0.3
info:
  title: Model API Gateway
  version: 1.0.0
servers:
  - url: http://localhost:3000
paths:
  /v1/models:
    get:
      summary: List models
      responses:
        '200':
          description: OK
  /v1/chat/completions:
    post:
      summary: OpenAI compatible chat completion
      responses:
        '200':
          description: OK
